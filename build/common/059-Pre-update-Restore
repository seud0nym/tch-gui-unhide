    cmp -s "$s" "$t"
    if [ $? -ne 0 ]
    then
      cp -f -p "$s" "$t"
    fi
  done
done
# Always remove Telstra AIR card
if [ -f /www/cards/010_fon.lp ]
then
  rm /www/cards/010_fon.lp
fi
# Always remove User Management card
if [ -f /www/cards/011_usermgr.lp ]
then
  rm /www/cards/011_usermgr.lp
fi

